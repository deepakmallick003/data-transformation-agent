# — Core runtime (always required) —
claude-agent-sdk==0.1.27
anyio==4.12.1
python-dotenv==1.2.1
bedrock-agentcore==1.1.4
bedrock-agentcore-starter-toolkit==0.2.9
boto3==1.42.40

# — Data processing (include when agent produces analysis output) —
pandas==3.0.0
numpy==2.4.2
matplotlib==3.10.8
seaborn==0.13.2

# — Athena tool (include when AGENT_TOOLS=athena) —
# sqlparse==0.5.5
